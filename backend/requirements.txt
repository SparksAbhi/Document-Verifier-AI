# SENTRY backend requirements (Python 3.11+)

# Core API server
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# Imaging + tampering forensics (Modules 1-3)
pillow>=10.4
numpy>=1.26
opencv-python>=4.10        # via deepface (replaces headless build)

# OCR (Module 1)
easyocr>=1.7.2             # pulls torch (CPU)

# Face verification (Module 4) — deepface + TF stack
deepface>=0.0.100
tf-keras>=2.16             # tensorflow 2.21 compatibility (deepface requires)

# Neon PostgreSQL
psycopg[binary]>=3.1
psycopg-pool>=3.2
